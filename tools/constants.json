{
    "db_params": {
        "user": "",
        "password": "",
        "host": "localhost",
        "database": "picresse"
    },

    "table_scripts": [
        "CREATE TABLE t_puzzles ( c_id INTEGER NOT NULL AUTO_INCREMENT, c_name VARCHAR(255) NOT NULL, c_creator VARCHAR(255) NOT NULL, c_width INTEGER NOT NULL, c_length INTEGER NOT NULL, c_puzzle VARCHAR(255) NOT NULL, c_date_created DATETIME DEFAULT CURRENT_TIMESTAMP, c_date_updated DATETIME NULL DEFAULT CURRENT_TIMESTAMP, c_date_deleted DATETIME NULL, c_is_warned BOOLEAN NOT NULL DEFAULT 0, c_times_completed INTEGER NULL, PRIMARY KEY (c_id) );",
        "SELECT c_id, c_name, c_creator, c_puzzle, c_width, c_length, c_date_created, c_is_warned, c_times_completed FROM t_puzzles LIMIT (?) OFFSET (?);",
        "SELECT c_id, c_name, c_creator, c_puzzle, c_width, c_length, c_date_created, c_is_warned, c_times_completed FROM t_puzzles WHERE (?) LIMIT (?) OFFSET (?);",
        "INSERT INTO t_puzzles (c_name, c_creator, c_puzzle, c_width, c_length) VALUES (?)",
        "UPDATE t_puzzles SET (?) WHERE (?)",
        "UPDATE t_puzzles SET c_times_completed= c_times_completed + 1 WHERE c_id=?"
    ]
}